name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: $(arn:aws:s3:::www-14ed-react-paper)
        AWS_ACCESS_KEY_ID: $(ASIA27CVURDEDRDSR56S)
        AWS_SECRET_ACCESS_KEY: $(o7U8xf/fU66pqIThV4ZRFc5aYS4UC8zSXFfjFU/3)
